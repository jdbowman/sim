// ArrayDimensions.h: Sets the dimensions for the brightness array which is calculated from the list of sources

///#define HALF_SIZE 3000	// the array is +-HALF_SIZE
///#define N_X (2*HALF_SIZE+1)	// odd so it can be symmetrical around 0
///#define N_Y N_X		// a square array

///#define MAX_X (+HALF_SIZE)
///#define MIN_X (-HALF_SIZE)
///#define MAX_Y (+HALF_SIZE)
///#define MIN_Y (-HALF_SIZE)

#define MAX_SOURCES 500 //00
